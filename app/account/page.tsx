import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Account",
  alternates: {
    canonical: "/account/",
  },
};

export default async function Page() {
  return <></>;
}
