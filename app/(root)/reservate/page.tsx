import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reserveren",
  alternates: {
    canonical: "/reservate/",
  },
};

export default async function Page() {
  return <></>;
}
